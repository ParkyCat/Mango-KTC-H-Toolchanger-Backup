# This file contains common pin mappings for the BIGTREETECH EBB36 V1.2.
# To use this config, the firmware should be compiled for the 
# STM32G0B1 with "8 MHz crystal" and "CAN bus (on PB0/PB1)".

# change the mcu section name to the tool board you are configuring:
# EBB0, EBB1, etc.
[mcu EBB0]
canbus_uuid: 8561083a1c5b
# [include T0.cfg]  #8561083a1c5b
# [include T1.cfg]  #b1096733285e
# [include T2.cfg]  #3213920eafe6
# [include T3.cfg]  #1cf3c0580751

# change the accelerometer section name to the tool you are configuring:
# T0, T1, etc.
[adxl345 T0]
cs_pin: EBB0:PB12
spi_software_sclk_pin: EBB0:PB10
spi_software_mosi_pin: EBB0:PB11
spi_software_miso_pin: EBB0:PB2
axes_map: x,z,-y

[temperature_sensor EBB0_MCU]
sensor_type: temperature_mcu
sensor_mcu: EBB0
min_temp: 0
max_temp: 100

# change to the extruder section you are configuring:
# extruder, extruder1, etc.
[extruder] # Orbiter2.5 with CHCB-V Hotend
step_pin: EBB0:PD0
dir_pin: EBB0:PD1
enable_pin: !EBB0:PD2
heater_pin: EBB0:PB13
sensor_pin: EBB0:PA3
microsteps: 16
rotation_distance: 53  # Re-calibrate your own value
# full_steps_per_rotation: 200
# max_extrude_only_distance: 101
gear_ratio: 44:10, 37:17
nozzle_diameter: 0.400
filament_diameter: 1.750
max_extrude_only_distance: 500
sensor_type: Generic 3950
pressure_advance: 0.030
pressure_advance_smooth_time: 0.015
control: pid #pid_Kp=38.927 pid_Ki=8.949 pid_Kd=42.332
pid_Kp: 38.927
pid_Ki: 8.949
pid_Kd: 42.332
min_temp: 0
max_temp: 305
min_extrude_temp: 0

# change the tmc2209 section name to match the extruder you are configuring:
# extruder, extruder1, etc.
[tmc2209 extruder] # Orbiter2.5
uart_pin: EBB0:PA15
run_current: 0.55
stealthchop_threshold: 999999999
sense_resistor: 0.11

# change the heater_fan section name to match the tool you are configuring:
# T0_hotend_fan, T1_hotend_fan, etc.
[heater_fan T0_hotend_fan]
pin: EBB0:PA0
heater: extruder # change to match the extruder you are configuring: extruder, extruder1, etc.
max_power: 1.0
kick_start_time: 3.0
heater_temp: 50.0
##  If you are experiencing back flow, you can reduce fan_speed
fan_speed: 0.8

# change the fan_generic section name to the tool you are configuring:
# T0_partfan, T1_partfan, etc.
[fan_generic T0_part_fan]
pin: EBB0:PA1

# change the tool section name to the tool you are configuring:
# T0, T1, etc.
[tool T0]
detection_pin: ^!EBB0:PB6
tool_number: 0 # change to the index of the tool. 0, 1, 2, etc.
extruder: extruder # change to match the extruder you are configuring: extruder, extruder1, etc.
fan: T0_part_fan
params_park_x: 0.55 # The absolute X-position of the tool in its dock.
params_park_y: 18.8 # The absolute Y-position of the tool in its dock.
params_park_z: 332.50 # The absolute Z-position where the tool and shuttle mate in the dock, determined when the TAP (or Z-probe) triggers.
gcode_x_offset: 0 # The X-Axis offset of the nozzle's orifice in relation to tool 0
gcode_y_offset: 0 # The Y-Axis offset of the nozzle's orifice in relation to tool 0
gcode_z_offset: 0 # The Z-Axis offset of the nozzle's orifice in relation to tool 0

# Results from input shaper
params_input_shaper_type_x: 'mzv'
params_input_shaper_freq_x: 58.6
params_input_damping_ratio_x: 0.070 
params_input_shaper_type_y: 'mzv'
params_input_shaper_freq_y: 38.6
params_input_damping_ratio_y: 0.056

# change the tool_probe section name to the tool you are configuring:
# T0, T1, etc.
[tool_probe T0]
pin: ^EBB0:PB6
tool: 0 # change to the index of the tool. 0, 1, 2, etc.
z_offset = -1.16 # Needs to be calibrated. More positive = More Squish 
speed: 5.0
samples: 3
samples_result: median
sample_retract_dist: 2.0
samples_tolerance: 0.02
samples_tolerance_retries: 3
activate_gcode:
    _TAP_PROBE_ACTIVATE HEATER=extruder # change to match the extruder you are configuring: extruder, extruder1, etc.

# change the neopixel section name to the tool you are configuring:
# T0, T1, etc.
# [neopixel T0]
# pin: EBB0:PD3
# color_order: GRBW
# chain_count: 23
# initial_RED: 0.5
# initial_GREEN: 0.0
# initial_BLUE: 0.5
# initial_WHITE: 0.0

# change the gcode_macro section name to the tool you are configuring:
# T0, T1, etc.
[gcode_macro T0]
variable_active: 0 # Must be zero.
variable_color: "" # Leave empty
gcode:
    SELECT_TOOL T=0 # change to the index of the tool. 0, 1, 2, etc.





# [extruder]
# step_pin: EBBCan: PD0
# dir_pin: EBBCan: PD1
# enable_pin: !EBBCan: PD2
# microsteps: 16
# rotation_distance: 53.494165  # Re-calibrate your own value
# gear_ratio: 44:10, 37:17
# nozzle_diameter: 0.400
# filament_diameter: 1.750
# max_extrude_only_distance: 500
# heater_pin: EBBCan: PB13
# sensor_type: Generic 3950
# sensor_pin: EBBCan: PA3
# pressure_advance: 0.030
# pressure_advance_smooth_time: 0.015
# #control: pid
# #pid_Kp: 21.527
# #pid_Ki: 1.063
# #pid_Kd: 108.982
# min_temp: 0
# max_temp: 305
# min_extrude_temp: 0



# [tmc2209 extruder]
# uart_pin: EBBCan: PA15
# run_current: 0.55
# stealthchop_threshold: 999999


# [fan]
# pin: EBBCan: PA1
# kick_start_time: 1
# off_below: 0.09

# [heater_fan hotend_fan]
# pin: EBBCan: PA0
# max_power: 1.0
# kick_start_time: 0.5
# heater: extruder
# heater_temp: 50.0
# ##  If you are experiencing back flow, you can reduce fan_speed
# fan_speed: 0.7



# [probe]
# pin: ^EBBCan: PB6
# #z_offset: 0
# speed: 7.0
# samples: 3
# samples_result: median
# sample_retract_dist: 3.0
# samples_tolerance: 0.009
# samples_tolerance_retries: 10

# activate_gcode:
#     {% set PROBE_TEMP = 150 %}
#     {% set MAX_TEMP = PROBE_TEMP + 5 %}
#     {% set ACTUAL_TEMP = printer.extruder.temperature %}
#     {% set TARGET_TEMP = printer.extruder.target %}

#     {% if TARGET_TEMP > PROBE_TEMP %}
#         { action_respond_info('Extruder temperature target of %.1fC is too high, lowering to %.1fC' % (TARGET_TEMP, PROBE_TEMP)) }
#         M109 S{ PROBE_TEMP }
#     {% else %}
#         # Temperature target is already low enough, but nozzle may still be too hot.
#         {% if ACTUAL_TEMP > MAX_TEMP %}
#             { action_respond_info('Extruder temperature %.1fC is still too high, waiting until below %.1fC' % (ACTUAL_TEMP, MAX_TEMP)) }
#             TEMPERATURE_WAIT SENSOR=extruder MAXIMUM={ MAX_TEMP }
#         {% endif %}
#     {% endif %}



## [safe_z_home]
## home_xy_position:175,175
## speed:100
## z_hop:5



#[temperature_sensor EBB_NTC]
#sensor_type: Generic 3950
#sensor_pin: EBBCan: PA2
#min_temp: -40
#max_temp: 100